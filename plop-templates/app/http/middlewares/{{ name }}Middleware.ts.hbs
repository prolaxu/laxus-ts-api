const {{ lastPartPascalCase name }}Middleware = async (req: Request, res: Response, next: NextFunction):Promise<NextFunction> => {
    // write your middleware here
   return next();
}
export default {{ lastPartPascalCase name }}Middleware;