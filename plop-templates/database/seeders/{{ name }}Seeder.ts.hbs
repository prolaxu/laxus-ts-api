import db from "@/core/db"

{{!-- if name have / then split name and  pascalCase last one  --}}

const {{ lastPartCamelCasePlural name }}Seeder =async()=>{
   
    // write your seeder here

}

export default {{ lastPartCamelCasePlural name }}Seeder;